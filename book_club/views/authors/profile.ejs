<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Author Profile - <%= author.username %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>Book Club</h1>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/books">Books</a></li>
        <% if (!session || !session.userId) { %>
          <li><a href="/login">Login</a></li>
          <li><a href="/register">Sign Up</a></li>
        <% } else { %>
          <li><a href="/logout">Logout</a></li>
        <% } %>
      </ul>
    </nav>
  </header>
  <main>
    <h2><%= author.username %>'s Profile</h2>
    <p><strong>Email:</strong> <%= author.email %></p>
    <p><strong>Bio:</strong> <%= author.bio || "No bio provided." %></p>
    <% if (session && session.userId && session.userId === author._id.toString()) { %>
      <a href="/authors/<%= author._id %>/edit">Edit Profile</a>
    <% } %>
  </main>
  <footer>
    <p>&copy; 2025 Book Club. All rights reserved.</p>
  </footer>
</body>
</html>
